在获取列表时，canread就要被赋上值，如果canrad=true答案也一起被发过来了。后台做的。
页面上，如果canrad=true，是红色。否则兰色
点击红色，直接打开答案。
点击兰色。弹二次确认扣积分按钮，确定后发送“获取答案请求接口”到后台。

获取答案请求接口：
判断积分够不够，不够就弹出微信支付窗口，进行支付。如果积分够，后端扣除积分后返回“scorePaySuccess”给前端。

前端：
收到：积分不够，弹出微信支付窗口，进行支付
收到：“scorePaySuccess”，弹出支付成功。

答案扣积分后，创建积分流水。两笔（消费者和作者）

支付宝扣款成功后，创建积分流水消费者，创建现金流水